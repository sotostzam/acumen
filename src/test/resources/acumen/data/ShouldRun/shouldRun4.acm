model A() always

model Main(sim)
  = initially
    mode  =  "M1" &
    c1  =  create A() &
    c2  =  create A()
  always
  switch mode
    case "M1"
      move c1 c2 &
      mode + == "M2"
    case "M2"
  end
 
