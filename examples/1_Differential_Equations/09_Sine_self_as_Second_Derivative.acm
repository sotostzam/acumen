
// Sine of Self as Second Derivative Example

class Main(simulator)
private
 x:=3.1; x':=0; x'':=0
end
 x'' = -10*sin(x)
end

// Note:  "More" non-linear than previous example
// Note:  We can still use Picard method to approximate
//          solutions