class Main(simulator)
private mode := "On"; x := 1; end
  simulator.endTime := 2.5;
  switch mode
    case "On"
      if 1 == 1
        mode := "Off"
      end;
      x = 1;
    case "Off"
      if 1 == 1
        mode := "On"
      end;
      x = -1
  end
end