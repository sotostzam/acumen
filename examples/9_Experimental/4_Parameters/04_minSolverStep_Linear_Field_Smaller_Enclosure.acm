/* minSolverStep linear field
 * Author: Jan Duracz
 * A smaller minSolverStep gives a smaller enclosure when fields are linear.
 */
class Main(simulator)
  private 
    mode := "on"; 
    x :=  0.1; x' := 1;
  end
  switch mode
    case "on" 
      x' = -x;
  end;
  simulator.endTime := 1;
  simulator.minSolverStep := 0.1;
  simulator.minLocalisationStep := 0.1;
  simulator.minComputationImprovement := 0; 
end
