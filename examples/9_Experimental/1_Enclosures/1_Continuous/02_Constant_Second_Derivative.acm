
// Constant Second Derivatives
// Polynomials are closed under integration
//
// Note on Acumen:  This example also introduces second
//  derivatives (like x'') and the setting of simulator
//  parameters endTime and minSolverStep.

class Main(simulator)
  private x:=-10; x':=0; x'':=0; mode := ""; end
  switch mode
    case ""
      x'' = 1
  end;
  simulator.endTime := 1.5;
  simulator.minSolverStep := 0.1;
end
