/* Exponential Decay
 * Autohors: Jan Duracz and Adam Duracz
 * A continuous model with exponential solution.
 */
class Main(simulator)
  private 
    mode := "on"; 
    x := 1; x' := 1;
  end
  switch mode
    case "on" 
      x' = -x;
  end;
  simulator.endTime := 10;
  simulator.minSolverStep := 0.1;
  simulator.minLocalizationStep := 0.1;
  simulator.minComputationImprovement := 0; 
end
