/* Damped Spring
 * Autohors: Jan Duracz and Adam Duracz
 * A continuous model of a damped spring mass.
 */
class Main(simulator)
  private 
    mode := "on"; 
    x   := 1; x'   := 1; x'' := 0;
  end
  switch mode
    case "on" 
      x'' = -x'-x;
  end;
  simulator.endTime := 5;
  simulator.minSolverStep := 0.01;
  simulator.minLocalizationStep := 0.1;
  simulator.minComputationImprovement := 0; 
end
