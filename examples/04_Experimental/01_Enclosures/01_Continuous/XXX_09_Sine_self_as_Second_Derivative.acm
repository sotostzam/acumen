
// XXX_Note: This is just waiting for Adam to debug the
//           the current implementation of sin().

// Sine of Self as Second Derivative Example

model Main(simulator) =
  initially
    x   = 3.1 & 
    x'  = 0   & 
    x'' = 0
  always
  x'' == -10*sin(x)
