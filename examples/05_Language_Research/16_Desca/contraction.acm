// Contracting continuous system example

model Main(simulator) =
initially 
  x = [-1 .. 1], x' = 1 
always x' = -x

// NOTE: Example of exponentially stable system.
//       The differential equation is exponentially stable,
//       enclosures will contract over time.