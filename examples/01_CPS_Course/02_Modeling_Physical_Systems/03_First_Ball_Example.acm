// Author: Merve

class ball (x0)
private x:=x0; x':=0; x'':=0 end

 if (x>0)
  x'' = -9.8
 else
  x'' = -9.8 - 100 * x
  end
end

class Main (simulator)
 private ball1 := create ball(10);
         ball2 := create ball(20)
 end
end
