/*
 * Modeling Discretization
 */
 
class Main (simulator)
  private x:=1; x':=0; x'':=0; sensor := 0; bucket := 0; bucket' := 1;  end
  bucket' = 1;
  if (bucket>0.05)
   bucket := 0; sensor := x
  end;
  x'' = -10*sensor
end

/* Exercise:  
 * Modify this model to determine 
 * the size of the biggest jump in the value of the sensor 
 * during the simulation.
 */