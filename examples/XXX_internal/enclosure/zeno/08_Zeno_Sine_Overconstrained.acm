/* Zeno Sine Overconstrained
 * Authors: Walid Taha and Adam Duracz
 */
class Main(simulator)
  private
    d := 1; d' := 0;
    s := 1/2; s' := 1/2; s'' := 0;
    mode := ""; 
  end
  switch mode
    case "" require s <= abs(d)
      if d == s 
        s := s/2; 
        mode := "" 
      end;
      d'  = -1;
      s'' = d - s - s';
  end;
  simulator.startTime := 0;
  simulator.endTime := 1.1;
  simulator.minTimeStepODEsolving := 0.1;
  simulator.minTimeStepLocalisation := 0.1;
  simulator.minImprovement := 0.1;
end