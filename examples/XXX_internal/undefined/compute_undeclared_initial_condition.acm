// Compute Undeclared Initial Condition Example

class Main(simulator)
  private
    x := 1; t := 0;
  end
  t' = 1;
  if t < 1
    x' = x;
  else
    x'' = -10
  end;
  simulator.endTime := 2.5
end

// Note: In this example, x' is computed before it is
//       used as an initial condition for the second-
//       order ODE.